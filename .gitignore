# --- 機密文件 (絕對不能上傳) ---
.env
service_account.json
SuperScaner.pem
*.pem

# golden/ = 領収書稅區分的手工驗證輸出。2026-07-21 生成 15 個 JSON，
# 內容為客戶真實票據的店名・登録番号・日期・金額 (20 份・17 店名・14 個 T 番號)。
# 本 repo 是 PUBLIC，真實票據資料絕不進入 —— test_images/ 也用它自己的
# .gitignore 全數排除，這裡是同一政策的延伸。
# 檔案已於 2026-08-19 刪除 (無任何代碼讀取)。規則刻意保留 —— 日後若再跑
# 一次同樣的驗證而重新生成，這條會自動擋住，不必再判斷一次。
golden/

# fixtures/ = Gemini 應答的錄音 (record-replay)。內容為客戶真實票據的
# 店名・金額・日期・卡號末 4 碼，以及 OCR 全文。與 golden/ 同性質。
# 本 repo 是 PUBLIC，真實票據資料絕不進入 —— 這條是 golden/ 政策的延伸。
# 番人測試 (test_gemini_record_replay.FixtureSecrecyTest) 檢查的不是
# 「規則是否存在」而是「git ls-files 是否為空」—— 規則只擋一般 add，
# 擋不住 git add -f 與已經 tracked 的檔案。
fixtures/

# --- 系統垃圾與緩存 ---
__pycache__/
*.pyc
.DS_Store

# --- 虛擬環境 ---
venv/
venv311/
.venv/

# --- 本地生成的臨時文件 ---
temp_downloads/
MF_Import_Data.csv
benchmark_results*.txt
.playwright-mcp/
# coverage.py 的計算暫存 (SQLite)。重跑即可再生成，不提交。
.coverage

# Claude Code のローカル状態（settings.local.json に SSH 鍵パスとサーバ IP、
# memory に顧客の Drive / Sheets 識別子が入る。CLAUDE.md の「密钥文件绝不提交」
# と同性質なので git 管理に載せない。2026-08-20 趙拍板）
.claude/
